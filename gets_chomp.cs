using System;

namespace GetsChompApplication
{
  class GetsChomp
  {
    static void Main(string[] args)
    {
      string userName = Console.ReadLine();
      Console.WriteLine("Username is: " + userName);
      // ReadLine() inputs as string just like gets.chomp
    }
  }
}